apiVersion:  v1
kind: Service
metadata:
  namespace: project-namespace
  name: wordpress-service
  labels:
    tier: frontend
    app: wordpress
spec:
  selector:
    app: wordpress
    tier: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 32200
  type: LoadBalancer