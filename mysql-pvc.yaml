apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: project-namespace
  name: mysql-pvc
  labels:
    app: wordpress
    tier: mysql
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 18Gi
  storageClassName: mysql-sc

  